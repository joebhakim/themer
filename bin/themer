#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cache_root="${XDG_CACHE_HOME:-$HOME/.cache}/themer"
binary_path="$cache_root/themer-dev"

mkdir -p "$cache_root"
cd "$repo_root"

go build -o "$binary_path" ./cmd/themer
exec "$binary_path" "$@"
